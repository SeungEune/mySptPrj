<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="codeMapper">

	<select id="selectCmmnCodeList" parameterType="String" resultType="egovframework.com.cmm.vo.CodeVO">
		SELECT	code_id,
		       	code,
		       	code_nm,
		       	code_dc,
		       	use_yn,
		       	TO_CHAR(regist_dt, 'YYYY-MM-DD') AS registDt
		FROM 	tb_cmmn_code_detail
		WHERE   code_id = #{codeId}
		and 	use_yn = 'Y'
		ORDER BY code_order
	</select>

	<select id="selectAllCodeIdList" resultType="String">
		SELECT code_id
		FROM tb_cmmn_code
		WHERE use_yn = 'Y'
		ORDER BY code_id
	</select>

	<!-- 권한 목록 조회 -->
	<select id="selectRoleList" resultType="egovframework.com.cmm.vo.RoleVO">
		SELECT role_cd,
		       role_nm,
		       role_dc,
		       use_yn
		FROM tb_role
		WHERE use_yn = 'Y'
		ORDER BY role_cd
	</select>
</mapper>